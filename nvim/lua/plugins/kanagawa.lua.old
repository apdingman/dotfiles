return {
  {
    "rebelot/kanagawa.nvim",
    config = function()
      require("kanagawa").setup({
        -- Customize options here if needed
        compile = false, -- If you want to compile the colorscheme for faster loading
      })
      vim.cmd("colorscheme kanagawa")
    end,
  },
}
